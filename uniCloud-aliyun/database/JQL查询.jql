// Run the first line to delete all records from birds.
// Then run the second line separately to confirm the collection is empty.

db.collection('birds').where({}).remove()

// db.collection('birds').count()
